#include<stdio.h>

// Comparison operators
// ==, !=, >, <, >=, <=

int main() {
    int age = 5;

    if(age >= 18) {
        printf("You are eligible to vote");
    } 
    else {
        printf("You are not eligible to vote");
    }
    
    return 0;
}